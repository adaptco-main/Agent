name: Secret File Policy

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  block-tracked-secret-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enforce secret file tracking policy
        run: ./scripts/check-tracked-secrets.sh
